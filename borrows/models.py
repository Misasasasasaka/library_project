from django.db import models
from django.conf import settings


# TODO: 创建Borrow借阅记录模型
# TODO: 添加外键：user(关联用户), book(关联图书)
# TODO: 添加字段：borrow_date(借阅时间), due_date(应还日期), return_date(实际归还时间)
# TODO: 添加字段：status(borrowed/returned/overdue三种状态)
# TODO: ...
